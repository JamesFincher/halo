# Design direction

Keep light (design system scaffold later).

Propose:

- Tone (utilitarian / playful / enterprise)
- Density (spacious / compact)
- Dark mode default yes/no
- Reference sites (0–3) if user cares

Do **not** bike-shed hex colors unless user insists — defaults later.

Lock: `intake.design`.
