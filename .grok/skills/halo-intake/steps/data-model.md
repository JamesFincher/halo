# Data model

Propose entities the app must **remember** — plain language fields, not SQL types.

Example: “Bookmark — url, title, notes, owner”.

Relationships in prose.

Confirm. Lock: `intake.data_model.entities[]`.
