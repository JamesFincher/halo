# In-scope features

Propose **4–8** headline features from brain dump. User-facing only.

AskUserQuestion:

- Accept list
- Add feature (free-form follow-up)
- Remove / merge (user specifies)

Lock: `intake.features[]` — array of `{id, title, one_liner}`.
