# Profile: nextjs-saas

Offline App Router skeleton:

- `app/page.tsx`, `app/layout.tsx`, `app/api/health/route.ts`
- `package.json` scripts: dev, build, start, test
- Health URL for Demo0: `http://127.0.0.1:3456/api/health`

Existing `package.json`: only adds health route if missing.
